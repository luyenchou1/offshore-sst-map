__pycache__/
*.pyc
*.pyo
*.pyd
*.pkl
*.sqlite3
.DS_Store
.cache/
.venv/
*.tmp
*.log
*.nc
Branding:Logo/
cache/
.env

# GotOne catch data — populated at runtime by scripts/sync_catches.py /
# the /api/catches/upload endpoint. NOT committed: this is a public repo and
# the internal build uses full-precision spot locations (privacy-sensitive).
data/gotone_catches.csv
data/gotone_species.csv
