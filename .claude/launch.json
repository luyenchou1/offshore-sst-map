{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "dev",
      "runtimeExecutable": "/usr/bin/env",
      "runtimeArgs": ["OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES", ".venv/bin/python", "-c", "from app import app; app.run(debug=False, port=8050, threaded=True)"],
      "port": 8050
    }
  ]
}
